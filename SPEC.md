# toy_browser_rs 仕様書(概要設計)

## 1. プロジェクト概要

### 1.1 目的

Rust によるブラウザ自作を通じて、HTML パース・CSS パース・スタイル解決・レイアウト・描画 という Web ブラウザのレンダリングパイプラインを理解することを目的とする、**教育用ミニマルブラウザ**である。

### 1.2 非目標(Non-Goals)

本プロジェクトでは以下は **対象外** とする。

- JavaScript エンジン(実行・DOM 操作・イベント)
- ネットワーク通信(HTTP/HTTPS フェッチ。当面はローカル `.html` ファイル読み込みのみ)
- インクリメンタルレイアウト・GPU アクセラレーション
- セキュリティ機構(CSP、Same-Origin Policy、サンドボックス)
- アクセシビリティツリー
- マルチプロセス・マルチスレッド化(Servo 風の分割)
- 完全な HTML5 / CSS3 仕様準拠

### 1.3 想定ユーザ

自分自身(学習者)。エンドユーザ向けプロダクトではない。

---

## 2. スコープ(対応サブセット)

### 2.1 HTML サブセット

- 要素タグ:`<html>`, `<head>`, `<body>`, `<div>`, `<p>`, `<span>`, `<h1>`〜`<h6>`, `<a>`(描画のみ)
- 属性:`id`, `class`, `style`(初期段階では `style` 属性は省略可)
- テキストノード
- コメント `<!-- ... -->`(パースして無視)
- DOCTYPE 宣言(読み飛ばし)
- **対象外**:`<script>`, `<table>`, `<form>`, インラインフォーマッティングの複雑なケース、自己終了タグの完全対応、文字参照(エンティティ)の完全対応

### 2.2 CSS サブセット

- セレクタ:タグセレクタ(`p`)、ID セレクタ(`#foo`)、クラスセレクタ(`.bar`)、それらの **単純な組み合わせ**(`div.bar`)
- プロパティ:
    - `display`: `block` | `inline` | `none`
    - `width`, `height`: `px` 単位、`auto`
    - `margin`, `padding`, `border-width`:`px` 単位、`auto`(margin のみ)
    - `background-color`, `color`, `border-color`:キーワード色(`red` 等)と `#rrggbb`
    - `font-size`:`px` 単位
- **対象外**:子孫/隣接セレクタ、擬似クラス・擬似要素、`flex`/`grid`、`%` 単位、`em`/`rem`、メディアクエリ、`@import`、継承の厳密な実装(必要最低限のみ)、カスケード優先順位の完全実装(詳細度の単純計算のみ)

### 2.3 入力ソース

- コマンドライン引数で受け取った **ローカル `.html` ファイル** を読み込み、同階層または `<link rel="stylesheet">` で参照される **ローカル `.css` ファイル** をスタイルソースとする。

### 2.4 出力

- `winit` で開いたデスクトップウィンドウへの 2D 描画。
- ウィンドウサイズ変更時の再レイアウト・再描画。

---

## 3. 非機能要件

| 項目             | 内容                                                                                                                                                          |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| プラットフォーム | Linux(WSL2 含む)、macOS、Windows(winit が対応する範囲)                                                                                                        |
| パフォーマンス   | 数百ノード規模のローカル HTML が体感即時に描画されること                                                                                                      |
| テスト           | 各モジュール単位の単体テスト(`#[cfg(test)]`)を中心とする                                                                                                      |
| 依存クレート方針 | レンダリングパイプライン本体(パース・スタイル・レイアウト)は **自作**。ウィンドウ・ピクセル描画・フォントラスタライズなど学習対象外の領域は既存クレートを使用 |

### 3.1 ビルド・実行方法

```bash
# ビルド
cargo build

# 実行(ローカル HTML ファイルを指定)
cargo run -- examples/hello.html

# テスト
cargo test
```

- CLI 引数が無い場合は使い方を表示して終了する。
- 引数で渡されたパスを基準に、HTML 内の相対パス(`<link href="...">` 等)を解決する。

### 3.2 エラーハンドリング方針

レイヤごとに方針を分ける。

| レイヤ                           | 方針                                                   | 理由                                                     |
| -------------------------------- | ------------------------------------------------------ | -------------------------------------------------------- |
| HTML / CSS パーサ                | 常に成功。不正トークンは読み飛ばすか最も近い解釈で続行 | robinson 流。学習用なので堅牢性より単純さを優先          |
| ファイル I/O                     | `Result<_, std::io::Error>`を返し`main`で表示して終了  | 失敗の原因(パス間違い等)をユーザに伝える必要がある       |
| レンダラ初期化(winit/softbuffer) | `Result<_, RenderError>`を返し`main`で表示して終了     | 環境依存の失敗が起こりうる                               |
| レイアウト / ペイント            | `Result`を返さない。前提崩れは`debug_assert!`で検出    | 入力(StyledNode)はパーサが整形済みなので失敗を想定しない |

- `panic!` はバグ検出用途のみ。ユーザ入力起因では使わない。

### 3.3 座標系・単位系

- **原点**:ウィンドウ左上 `(0, 0)`。
- **Y 軸**:下向きが正(一般的な 2D グラフィクスと同様)。
- **内部単位**:`f32` の **論理ピクセル(CSS px)**。レイアウト計算は全て `f32`。
- **物理ピクセル変換**:描画直前にレンダラが行う。MVP では `1 論理 px = 1 物理 px` 固定(DPR=1)。
- **HiDPI 対応**:MVP では対象外。`winit` の `scale_factor` は受け取るが `1.0` とみなす。M6 以降の拡張余地とする。
- **座標精度**:整数化はレンダラ層で `round()` のみ行い、レイアウト層には持ち込まない。

### 3.4 フォント方針

- **MVP は同梱フォント 1 種類固定**とする。システムフォント検索は行わない。
- 採用:`Noto Sans` の Regular(SIL OFL ライセンス、再配布可)。
- 配置場所:`assets/fonts/` 配下に置き、`include_bytes!` でバイナリに埋め込む。
- 太字 / イタリックは MVP では同じ字形で代用(`font-weight` / `font-style` は無視)。
- ライセンス表記は `README.md`(将来作成)および同梱ファイルで明示する。

---

## 4. 技術選定

### 4.1 採用しない選択肢:Tauri

Tauri は OS の WebView(WebKit / WebView2)に依存するため、**レンダリングエンジン自作という目的に合致しない**。本プロジェクトでは採用しない。

### 4.2 採用クレート

| 領域                           | クレート             | 役割                                                    |
| ------------------------------ | -------------------- | ------------------------------------------------------- |
| ウィンドウ管理・イベントループ | `winit`              | OS ウィンドウの生成、キーボード/マウス/リサイズイベント |
| ピクセルバッファ               | `softbuffer`         | ウィンドウへのソフトウェアピクセル転送                  |
| 2D 描画                        | `tiny-skia`          | 矩形・パスの塗りつぶし(背景・ボーダー描画)              |
| フォントラスタライズ           | `fontdue`            | テキストのグリフ生成                                    |
| HTML/CSS パース                | (自作)               | 学習対象のため自作                                      |
| ロギング                       | `log` + `env_logger` | デバッグ出力                                            |

> 注:具体的なバージョン固定は実装着手時に Cargo.toml で行う。本仕様書では選定方針のみ示す。

---

## 5. アーキテクチャ

### 5.1 全体構成図(レンダリングパイプライン)

```
┌─────┐   ┌──────┐   ┌─────┐
│HTML file │─▶│HTML Parser │─▶│DOM Tree  │─┐
└─────┘   └──────┘   └─────┘  │
                                                    ├┐
┌─────┐   ┌──────┐   ┌─────┐  ││
│CSS file  │─▶│CSS Parser  │─▶│Stylesheet│─┘│
└─────┘   └──────┘   └─────┘    │
                                                      │
                                                      ▼
                                                 ┌────┐
                                                 │Style   │  DOM × CSS → StyledNode
                                                 └────┘
                                                      │
                                                      ▼
                                                 ┌────┐
                                                 │Layout  │  StyledNode → LayoutBox
                                                 └────┘
                                                      │
                                                      ▼
                                                 ┌────┐   ┌──────┐   ┌────┐
                                                 │Paint   │─▶│DisplayList │─▶│Renderer│─▶ Window
                                                 └────┘   └──────┘   └────┘
```

### 5.2 データフロー

1. **入力**:CLI 引数で受け取った HTML パスを読み込む。
2. **HTML パース**:文字列 → DOM ツリー(`Node` の木)。
3. **CSS パース**:`<link>` 参照または埋め込み `<style>` → スタイルシート(ルールの集合)。
4. **スタイル解決**:DOM 各ノードに該当ルールをマッチさせ、`StyledNode`(プロパティマップ付き)を構築。
5. **レイアウト**:`StyledNode` から `LayoutBox` ツリーを構築し、ボックスモデルに従って位置・サイズを確定。
6. **ペイント**:`LayoutBox` を走査して `DisplayCommand`(矩形塗りつぶし、テキスト描画、ボーダー)のリストを生成。
7. **レンダリング**:ディスプレイリストを `tiny-skia` で `softbuffer` のピクセルバッファに描き、`winit` ウィンドウへ提示。
8. **イベントループ**:リサイズ時はレイアウト以降を再実行。

---

## 6. モジュール構成

`src/` 配下を以下のように分割する。各モジュールは単一責任の原則に従う。

```
src/
├── main.rs            # エントリポイント。CLI 引数解析、winit イベントループ起動
├── lib.rs             # 各モジュールの公開(統合テスト用)
├── dom.rs             # DOM のデータ構造(Node, NodeType, ElementData)
├── html.rs            # HTML パーサ(文字列 → DOM)
├── css.rs             # CSS のデータ構造 + CSS パーサ
├── style.rs           # スタイル解決(DOM × CSS → StyledNode)
├── layout.rs          # レイアウト計算(StyledNode → LayoutBox)
├── painting.rs        # ペイント(LayoutBox → DisplayList)
└── renderer/          # 描画バックエンド
    ├── mod.rs         # Renderer トレイト(差し替え可能に)
    └── window.rs      # winit + softbuffer + tiny-skia 実装
```

### 6.1 各モジュールの責務

| モジュール | 主な型                                                                                    |
| ---------- | ----------------------------------------------------------------------------------------- |
| `dom`      | `Node`, `NodeType { Element(ElementData), Text(String), Comment(String) }`, `ElementData` |
| `html`     | `Parser`                                                                                  |
| `css`      | `Stylesheet`, `Rule`, `Selector`, `Declaration`, `Value`, `Color`                         |
| `style`    | `StyledNode<'a>`, `PropertyMap`, `Display`                                                |
| `layout`   | `LayoutBox<'a>`, `BoxType`, `Dimensions`, `Rect`, `EdgeSizes`                             |
| `painting` | `DisplayCommand`, `DisplayList`                                                           |
| `renderer` | `Renderer`(trait)                                                                         |

---

## 7. マイルストーン

学習効果を最大化するため、**動くものを段階的に育てる** 方針で進める。

### M1: DOM 構築まで

- HTML サブセットのパーサ実装。
- CLI で `.html` を読み、`Debug` 出力で DOM ツリーを表示できる。
- 単体テスト:代表的な入力でツリー構造を検証。

### M2: CSS パース + スタイル解決

- CSS サブセットのパーサ実装。
- スタイル解決(タグ/ID/クラスセレクタのマッチ、詳細度ソート)。
- `StyledNode` をデバッグ表示できる。

### M3: ブロックレイアウト

- `display: block` 要素のみのレイアウト計算(width / height / margin / padding / border)。
- ウィンドウサイズを固定値とした矩形配置までを `println!` で検証。

### M4: 描画(矩形のみ)

- `winit` + `softbuffer` + `tiny-skia` で背景色・ボーダーを描画したウィンドウ表示。
- リサイズで再レイアウト。

### M5: テキスト描画(MVP 完成)

- `fontdue` でテキストを描画。`font-size`, `color` を反映。

### M6(拡張余地):インラインレイアウト・`<link>` 対応・スクロール

- 余裕があれば着手。MVP の完成は M5 を目標とする。

---

## 8. ディレクトリレイアウト(プロジェクト全体)

```
toy_browser_rs/
├── Cargo.toml
├── Cargo.lock
├── SPEC.md              # 本仕様書
├── README.md            # 使い方(将来作成)
├── assets/
│   └── fonts/          # 同梱フォント(Noto Sans Regular)
├── examples/            # 動作確認用の .html / .css サンプル(M1 以降で追加)
│   ├── hello.html
│   └── hello.css
└── src/
    ├── main.rs
    ├── lib.rs
    ├── dom.rs
    ├── html.rs
    ├── css.rs
    ├── style.rs
    ├── layout.rs
    ├── painting.rs
    └── renderer/
        ├── mod.rs
        └── window.rs
```

---

## 9. オープン課題 / 今後の決定事項

- CSS の継承プロパティ(`color`, `font-size` 等)を厳密に実装するか、簡易実装で済ませるか。
- インラインボックスのライン分割(改行)アルゴリズムの簡略度。
- エラー表示 UI(現状は標準エラー出力のみを想定)。

---

## 10. 参考文献

- mbrubeck, "Let's build a browser engine!" シリーズ(robinson)
- HTML Living Standard(参考レベル。完全準拠はしない)
- CSS 2.1 Visual formatting model(ボックスモデルの参考)

---

## 11. 技術概要メモ(早見表)

| 項目             | 内容                                                |
| ---------------- | --------------------------------------------------- |
| 言語             | Rust(edition 2024)                                  |
| ビルドツール     | Cargo                                               |
| ウィンドウ       | `winit`                                             |
| ピクセル転送     | `softbuffer`                                        |
| 2D 描画          | `tiny-skia`                                         |
| フォント         | `fontdue` + 同梱 Noto Sans Regular(OFL)             |
| ロギング         | `log` + `env_logger`                                |
| 自作モジュール   | `dom`, `html`, `css`, `style`, `layout`, `painting` |
| 既存クレート任せ | ウィンドウ管理、ピクセル描画、フォントラスタライズ  |
| テスト           | `#[cfg(test)]` 単体テスト中心                       |
| JS エンジン      | **なし**(Non-Goal)                                  |
| ネットワーク     | **なし**(Non-Goal、ローカルファイルのみ)            |
